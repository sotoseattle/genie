require 'spec_helper'

describe Person do
  
  # context "#initialize" do
  #   it "fails without a name" do
  #     expect {Person.new}.to raise_error
  #     expect {Person.new(alleles:["f", "F"])}.to raise_error
  #   end
  #   subject{Person.new(name:'pepe', alleles:["f", "F"])}

  #   it {is_expected.to respond_to(:name)}
  #   it {is_expected.to respond_to(:phenotype)}
  #   it {is_expected.to respond_to(:gen_dad)}
  #   it {is_expected.to respond_to(:gen_mom)}
  #   its(:factor) {is_expected.to be_nil}
  # end

  # context "#was_born_to" do
  #   subject{Person.new(name:'pepe', alleles:["f", "F"])}
  #   it "receives a hash with :dad and :mom as arguments" do
  #     subject.was_born_to({:dad=>'aniceto', :mom=>'agata'})
  #   end

  # end

end